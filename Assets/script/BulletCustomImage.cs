using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BulletCustomImage : MonoBehaviour
{
    // 打つ時の画像はここを参照する
    public string shootFileName;

    // Start is called before the first frame update
    void Start()
    {
        shootFileName = "";
    }
}
